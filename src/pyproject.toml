[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "YTMusicManager"
version = "0.1.0"
dependencies = [
    "mutagen>=1,<2",
]

[project.scripts]
yt-music = "YTMusicManager:main"
